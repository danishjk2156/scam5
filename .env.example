# ─────────────────────────────────────────────────────────────
#  Agentic Honeypot — Environment Variables
#  Copy this file to .env and fill in your values
#  $ cp .env.example .env
# ─────────────────────────────────────────────────────────────

# ── API Security ──────────────────────────────────────────────
# Secret key sent in the X-Api-Key header on every request.
# Change this to a strong random string before deploying.
SECRET_KEY=change_me_to_a_strong_random_secret

# ── Google Gemini ─────────────────────────────────────────────
# Get your key at: https://aistudio.google.com/app/apikey
# Billing must be enabled for production use (free tier = 500 req/day).
# Model used: gemini-2.5-flash
GEMINI_API_KEY=your_gemini_api_key_here

# ── Supabase ──────────────────────────────────────────────────
# Find these in: Supabase Dashboard → Project Settings → API
# Use the service_role key (not anon key) for server-side access.
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your_supabase_service_role_key_here